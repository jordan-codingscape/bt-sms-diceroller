{
  "name": "bt-sms-diceroller",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "chance": "^1.1.7",
    "parsimmon": "^1.16.0"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}
